"use client";

// This component is kept for compatibility but functionality is now in TimerPage
// Controls are rendered directly in the timer page for better integration

export default function TimerControls() {
  return null;
}
